# app/frontend — identity auth portal

Static auth UI for Legendary OS (`agentsam identity init` layout).

Shipped into `frontend/dist/` via `scripts/sync-auth-portal.mjs` on build/deploy.

Do not edit copies under `frontend/public/` — source of truth is here.
