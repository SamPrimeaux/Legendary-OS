# IAM dashboard CMS baseline (reference)

Partial transplant from Inner Animal Media dashboard CMS UI. **Not part of Legendary production build.**

- Typecheck scope: `CmsSiteSwitcher.tsx` only (`pnpm check:baseline` from `iaminfra/cms`).
- Other `.tsx` files in this tree import IAM-only modules that are intentionally not vendored here.
- Legendary CMS UI: `frontend/src/cms/`.
